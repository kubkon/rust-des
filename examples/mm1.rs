use std::time::duration::Duration;

fn main() {
    println!("Example: simulate M/M/1 queue");

    let duration = Duration::seconds(10);
    println!("{}", duration);
    // sim = SimulationEngine::new(duration);
}
